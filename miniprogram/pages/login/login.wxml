<view class="page-shell login-page">
  <view class="login-shell">
    <view class="login-card">
      <view class="login-brand">
        <view class="brand-mark">P</view>
        <view class="brand-name">{{text.brandName}}</view>
      </view>

      <view class="login-copy">
        <view class="login-title">{{text.title}}</view>
        <view class="login-desc" wx:if="{{text.desc}}">{{text.desc}}</view>
      </view>

      <view class="login-form">
        <button
          class="btn-primary-solid login-submit manual-submit {{loading ? 'is-loading' : ''}}"
          bindtap="handleLogin"
          disabled="{{loading}}"
        >
          <text>{{loading ? text.submitting : text.submit}}</text>
        </button>
      </view>

      <view class="login-actions">
        <button class="login-action-button" bindtap="goToRegister">
          {{text.register}}
        </button>
        <view class="login-action-divider"></view>
        <button class="login-action-button" bindtap="goToAdminLogin">
          {{text.adminLogin}}
        </button>
      </view>
    </view>
  </view>
</view>

<privacy />
